import styles from './List.module.css'import ListItem from "../ListItem/ListItem";export default function List(props) {	const {list_arr,removeItem} = props;	const displayList = () => {		if (list_arr.length === 0) {			return (<h3 className={styles.no_list}>No List</h3>)		}		return  list_arr.map(item =>			<ListItem				key={item.id}				list_item={item}				upItemId={removeItem}			/>		)	}	return (		<ul className={styles.list}>			{displayList()}		</ul>	)}