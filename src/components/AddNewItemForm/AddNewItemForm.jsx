import styles from './AddNewItemForm.module.css'import {useState} from "react";export default function AddNewItemForm(props) {	const {setNewTaskValue} = props	const [inputValue, setInputValue] = useState('');	const [formValid, setFormValid] = useState(true);	const inputChangeHandler = (e) => {		setInputValue(e.target.value)		if (inputValue.trim().length > 0) {			setFormValid(true);		}	}	const onSubmitHandler = (e) => {		e.preventDefault();		if (inputValue.trim().length === 0) {			setFormValid(false);			return;		}		setNewTaskValue(inputValue)		setInputValue('')	}	const addClass = () => {		return 	`${styles.form} ${formValid ? '' : styles.invalid}`	}	return (		<form className={addClass()} onSubmit={onSubmitHandler}>			<label htmlFor="i_text">Task name</label>			<input				type="text"				id='i_text'				onChange={inputChangeHandler}				value={inputValue}			/>			<button type="submit">Add Task</button>		</form>	)}